[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "16" # Ensure you use a compatible Node.js version for your project

[build.processing]
  skip_processing = true